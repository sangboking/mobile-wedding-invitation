# 네이버 지도(NCP Maps) Client ID
# https://console.ncloud.com → AI·NAVER API → Maps → Application 등록 후 발급
# 발급받은 키를 아래에 넣고 파일명을 .env.local 로 복사하세요.
NEXT_PUBLIC_NCP_KEY_ID=

# Supabase (방명록) — 프로젝트 Settings → API 에서 확인
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
