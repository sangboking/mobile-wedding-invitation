/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Vercel 배포 기준 설정입니다.
  // 만약 정적 호스팅(GitHub Pages 등)으로 옮기려면 아래 두 줄의 주석을 해제하세요.
  // output: 'export',
  // images: { unoptimized: true },
};

export default nextConfig;
